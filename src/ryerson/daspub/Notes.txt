--------------------------------------------------------------------------------
DAS ARCHIVE PUBLISHER
--------------------------------------------------------------------------------

0.4.0 o Attempt to package mobile output as a self-contained app
      o Include video
      o Interactive and automatic slideshow presentation
      o Assignment pages have indexes for multi-page PDFs
      o Improve reporting
      o Make the report page public and obvious to all staff, faculty

Current Work:

0.3.0 o Assignment pages have thumbnails for videos
      o Move all HTML export functions into classes separate from the model classes
      o Custom log formatter, log file output
      o Do all TODO tasks
      o Need web authentication for artifact details lookup
      x APR is converted to JPG image gallery (done manually in static file folder)
      x APR image index is built automatically (done with python script)

0.2.0 x build course page with index of assignments
      x build assignment page with index of high and low pass submissions
      x build all thumbnails for images
      - test security for artifact web accessible content
        * needs an authentication form and session tracking
            * web app and some other URL scheme
      x break up all operations into separate command line options, then execute 
        all options independently
      x need to make sure that all output in assignment folders is JPG

0.1.0 x Generates a basic mobile site
      x Artifact lookup
      x Artifact PDF sheet
      x Insert current APR into review content
      x Break up the publish command into: mobile gallery, artifact gallery, web 
        presentation, etc. modes

--------------------------------------------------------------------------------
NOTES

-- items for improvement
    o Submission should be able to provide both output file names and paths?

-- need a PDF to JPG method that deals with single and multi-page output options
   also, need to deal with output file name
